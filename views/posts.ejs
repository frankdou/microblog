<% posts.forEach(function(post, index) { 
    if (index % 3 == 0) { %>
        <div class="row">
    <% } %>
    <div class="span4">
        <h2>
            <a href="/u/<%= post.userName %>"><%= post.userName %></a> 说
        </h2>
        <p><%= post.post %></p>
        <p><small><%= post.time %></small></p>
    </div>
    <% if (index % 3 == 2) { %>
        </div>
    <% } %>
<%}) %>
<% if (posts.length % 3 != 0) { %>
    </div>
<% } %>